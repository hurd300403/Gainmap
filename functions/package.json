{
  "name": "gainmap-functions",
  "version": "1.0.0",
  "private": true,
  "description": "Gainmap sync backend — admission, upload reservations, usage reconciliation, maintenance, account deletion",
  "main": "index.js",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "node --test --test-reporter=spec test/patreon.test.mjs test/rules.test.mjs test/functions.test.mjs"
  },
  "dependencies": {
    "firebase-admin": "^14.2.0",
    "firebase-functions": "^7.3.2"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^4.0.1",
    "firebase": "^11.1.0"
  }
}
